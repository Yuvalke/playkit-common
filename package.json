{
  "name": "playkit-common",
  "version": "1.5.2",
  "main": "index.js",
  "repository": "https://github.com/Yuvalke/playkit-common.git",
  "author": "Yuval keidar <yuval.keidar@kaltura.com>",
  "scripts": {
    "release": "standard-version",
    "publish": "git push --follow-tags",
    "commit:dist": "git add package.json CHANGELOG.md && (git commit -m 'chore: update dist' -m '[skip ci]' || exit 0)"
  },
  "license": "MIT",
  "devDependencies": {
    "conventional-github-releaser": "^1.1.13",
    "standard-version": "^4.0.0"
  }
}
